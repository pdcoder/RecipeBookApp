export class Recipe{
    public name: string;
    public description: string;
    public imagePath: string;

    constructor(nmae:string, desc:string,imagePath:string)
    {
        this.description =desc;
        this.imagePath = imagePath;
        this.name = name;
    }
}